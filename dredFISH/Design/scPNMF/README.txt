# set up R
# scPNMF requires R version >= 4; which can be set up in a conda env

# conda create -n renv r-essentials r-base
# install.packages("devtools") 

# if `gert` went wrong, install it from conda, and reinstall devtools
# install specific packages after that